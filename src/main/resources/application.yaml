server:
  port: 8077

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2
      settings:
        web-allow-others: true
  datasource:
    driver-class -name: org.h2.Driver
    url: jdbc:h2:mem:testdb

